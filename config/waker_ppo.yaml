# env_id: WalkerBalance-v0
use_gui: false
test_mode: false
use_disturbances: false
seed: 42
policy: 'MlpPolicy'
total_timesteps: 100000
learning_rate: 0.0003
n_steps: 2048
batch_size: 64
n_epochs: 10
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
ent_coef: 0.0
vf_coef: 0.5
max_grad_norm: 0.5

tensorboard_log: ./ppo_tensorboard_walker/
project: walker-balance
algo_name: ppo
run_name: ppo_run
eval_freq: 5000
eval_episodes: 1000
save_freq: 10000
max_steps: 500
